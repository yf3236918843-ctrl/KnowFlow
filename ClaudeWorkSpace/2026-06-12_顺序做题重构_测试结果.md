## 测试结果 — 顺序做题重构

### 测试概要
- 测试时间：2026-06-12
- 测试方式：Playwright 浏览器真实点击、截图、网络请求、控制台日志、前后端代码审计
- 测试结论：未通过
- 最新服务状态：未重启；沿用端口 `127.0.0.1:5003`，监听 PID `16672`
- 最新浏览器证据：`sequence-rootcause-role-pollution.png`

### 最新复测结论
本轮不重启服务器，继续在 `session_id=101` 上测试并审计代码。旧的 `ChatView is not defined` 和 `Unexpected token '}'` 已不再实时出现，但顺序做题主链路仍未通过。核心根因不是单个 UI 展示问题，而是“后端把内部角色切换指令持久化成 user 消息 + LLMGateway 每次把完整 messages 发给模型”，导致模型上下文被系统指令污染；前端只做显示过滤，不能阻止后端上下文污染。

### 浏览器复测结果
| # | 操作 | 预期结果 | 实际结果 | 状态 |
|---|------|---------|---------|------|
| 1 | 打开 `main.html?ts=20260612_after_restart&session_id=101` | URL 带 `session_id` 自动进入对应 exercise 会话 | 页面停在首页/会话列表，必须手动点击左侧会话 | ❌ |
| 2 | 点击左侧 `#101 · exercise` | 完整恢复题目、题显框、历史消息和输入框 | 恢复做题骨架；题显框出现 `📍跳转`，但题目内容变成整段系统 prompt/角色规则 | ❌ |
| 3 | 输入“重启后再测试一次提示”并发送 | 教师角色基于当前题目给提示 | 后端返回总结员口吻内容，明确提到“收到连续4条系统指令”，证明角色上下文污染 | ❌ |
| 4 | 点击扩展 → 生成偏好 | 进入偏好模式，产出偏好卡片或明确 empty 卡片 | 能触发流式输出，但输出基于被污染历史；无偏好卡片，只显示“无实质教学互动记录” | ❌ |
| 5 | 点击扩展 → 收录错题 | 进入薄弱点诊断，产出可确认错题卡片 | 返回 `collect_draft` 空 items；页面只显示文本/空结果，无可验收卡片 | ❌ |
| 6 | 扩展菜单视觉 | 菜单应稳定显示在扩展按钮上方 | 菜单可打开，但定位依赖 `.input-area` 而非 `.input-extras-wrap`，容易误判/布局脆弱 | ⚠️ |
| 7 | 控制台 | 无资源错误 | 仍有 `Process/test/view.js` 404 | ❌ |

### 网络证据
| 请求 | 请求体 | 响应要点 | 结论 |
|---|---|---|---|
| #28 | `session.get {id:101}` | messages 含 1 条巨大 `system` prompt + 4 条 `user` 的 `【系统指令】切换至角色:...` | 角色切换指令已被持久化为用户消息 |
| #37 | `exercise.chat {session_id:101,message:"重启后再测试一次提示"}` | 模型输出“收到连续4条系统指令...最后一条为总结员” | 普通聊天被历史系统指令污染，未保持教师做题角色 |
| #38 | `exercise.preference {session_id:101}` | 输出 empty/无偏好，理由是“无实质教学互动记录” | 偏好分析输入历史已被污染，无法基于真实做题上下文 |
| #39 | `exercise.collect {session_id:101}` | 输出 `collect_draft`，但 `items: []` | 错题收录无法产出可确认卡片 |

### 代码审计定位
| 问题 | 根因定位 | 影响 |
|---|---|---|
| 角色污染 | `Project/core/pipeline/strategies/exercise_workflow.py:194` 的 `switch_role` 把 `【系统指令】...` 追加为 `user` 消息并持久化；`Project/core/llm_gateway/__init__.py:463`-`472` 每次请求把完整 `_messages` 发给 LLM | 模型把历史内部指令当用户上下文，普通聊天变成总结员/偏好分析师等错误角色 |
| 前端过滤无效 | `Project/web/test/FuncViewManager/FuncViews/ChatView/ChatView/js/ChatView.js:41`-`48` 只过滤 UI 显示；不影响后端发给 LLM 的 messages | 页面看不到内部指令，但模型仍会看到，形成隐蔽污染 |
| 题显框显示系统 prompt | `Project/web/test/FuncViewManager/FuncViews/ChatView/Process/exercise/view.js:581`-`586` 恢复时取第一条 `system` 消息作为 `entryContent` | 恢复后题显框显示整段系统 prompt，而不是题目 |
| start/summary 数据结构不够恢复 | `Project/core/pipeline/strategies/exercise_workflow.py:137` start 只写系统 prompt；`Project/core/pipeline/strategies/exercise_workflow.py:146`-`149` 只在 SSE 返回 question，不持久化题目展示数据；summary 新 session 在 `Project/core/pipeline/strategies/exercise_workflow.py:462`-`463` 又混写 prompt/题目 | 刷新/恢复不能可靠从 messages+extern 复原题目 |
| URL 不自动恢复 | `Project/web/test/main.html:150` 之后初始化只加载侧栏/快捷入口，没有根据 URL `session_id` 自动 `ViewManager.show(_exerciseView, ...)`；只有点击侧栏时 `main.html:339`-`340` 才进入 exercise view | 带 `session_id` 刷新后停在首页，违反会话恢复预期 |
| 无题状态卡加载 | `Project/web/test/FuncViewManager/FuncViews/ChatView/Process/exercise/view.js:72`-`75` 先插入“正在加载题目...”；`view.js:108`-`110` 对 result 仅通知，不清理加载气泡/恢复明确终态 | `exercise.start` 返回“所有题目已完成”时页面残留加载态 |
| 模式切换竞态 | `Project/web/test/FuncViewManager/FuncViews/ChatView/Process/exercise/view.js:212`-`215`、`292`-`295`、`376` 直接 `api.call('exercise.switch_role', ...)` 但不消费 stream | 角色切换请求的完成/失败不可见，且会持久化污染消息 |
| 卡片确认仍有风险 | `Project/web/test/FuncViewManager/FuncViews/ChatView/Process/exercise/view.js:400`-`412`、`433`-`445` 已消费 confirmed/error，但没有 pending 防重复、失败回滚、按钮处理中状态 | 连续点击/失败场景仍不满足验收 |
| confirm_preference 状态不一致 | `Project/core/pipeline/strategies/exercise_workflow.py:390`-`399` 只有 `filtered and pe` 且保存成功才设置 confirmed，但 `exercise_workflow.py:409` 无论保存是否成功都会 yield confirmed | 可能前端显示已收录，但后端 extern 未确认 |
| conversation 格式化未过滤指令 | `Project/core/pipeline/strategies/exercise_workflow.py:576`-`583` 只跳过 `system`，不跳过 `user` 中的 `【系统指令】`/`【系统提示:...】` | 后台总结/偏好也会把内部指令当学生发言 |
| 测试入口 404 | `Project/web/test/main.html:145` 引入 `FuncViewManager/FuncViews/ChatView/Process/test/view.js`，实际文件不存在 | 控制台持续 404，测试入口不可用 |

### Bug 清单
| # | 描述 | 严重程度 | 涉及模块 | 处理方式 |
|---|------|---------|---------|---------|
| 1 | 内部角色切换指令被持久化为 `user` 消息，并随完整 history 发给 LLM，导致做题聊天、偏好、错题、总结全部上下文污染。 | 高 | `Project/core/pipeline/strategies/exercise_workflow.py:194`、`Project/core/llm_gateway/__init__.py:463` | 修改 |
| 2 | 前端恢复会话时把第一条 `system` prompt 当题目内容，题显框显示整段系统规则。 | 高 | `Project/web/test/FuncViewManager/FuncViews/ChatView/Process/exercise/view.js:581` | 修改 |
| 3 | URL 带 `session_id` 不会自动进入会话，只高亮侧栏，必须手动点击。 | 高 | `Project/web/test/main.html:150` | 修改 |
| 4 | `exercise.start` 无题时页面残留“正在加载题目...”，没有明确终态。 | 高 | `Project/web/test/FuncViewManager/FuncViews/ChatView/Process/exercise/view.js:72`、`Project/web/test/FuncViewManager/FuncViews/ChatView/Process/exercise/view.js:108` | 修改 |
| 5 | `switch_role` 请求前端不消费 stream，不知道成功/失败；且后端把切换指令混入用户消息。 | 高 | `Project/web/test/FuncViewManager/FuncViews/ChatView/Process/exercise/view.js:212`、`Project/core/pipeline/strategies/exercise_workflow.py:178` | 修改 |
| 6 | 后台总结 `_fmt_conv` 未过滤 `user` 中的内部系统指令/系统提示。 | 高 | `Project/core/pipeline/strategies/exercise_workflow.py:576` | 修改 |
| 7 | start/summary 没有把可恢复的题目结构稳定写入 extern/messages，恢复只能靠猜 messages。 | 高 | `Project/core/pipeline/strategies/exercise_workflow.py:120`、`Project/core/pipeline/strategies/exercise_workflow.py:450` | 修改 |
| 8 | `confirm_preference` 保存失败也可能返回 confirmed，前后端状态不一致。 | 高 | `Project/core/pipeline/strategies/exercise_workflow.py:390`、`Project/core/pipeline/strategies/exercise_workflow.py:409` | 修改 |
| 9 | 卡片确认缺少 pending/防重复点击/失败回滚完整交互。 | 中 | `Project/web/test/FuncViewManager/FuncViews/ChatView/Process/exercise/view.js:400`、`Project/web/test/FuncViewManager/FuncViews/ChatView/Process/exercise/view.js:433` | 修改 |
| 10 | `Process/test/view.js` 仍 404。 | 低 | `Project/web/test/main.html:145` | 修改/移除入口 |
| 11 | `workspace/start_server.py` 路径计算错误，不能直接启动服务。 | 低 | `WorkSpace/WorkSpace/OpenCode/agents/测试员/workspace/start_server.py:11` | 修改测试工具 |

### 已修复/状态变化
- `ChatView.js:41` 的 `ChatView is not defined` 初始化错误：最新复测未再出现。
- `exercise/view.js` 的 `Unexpected token '}'`：最新复测未再出现。
- 偏好/错题接口本轮未复现 HTTP 500；当前主要问题转为上下文污染和恢复逻辑错误。

### 耦合检查结果
- [x] 是否修改了其他模块的代码？是，涉及后端 workflow、LLMGateway、前端 ChatView/Exercise view/main.html。
- [x] 是否有“为了模块 B 改了模块 A”的情况？有。前端过滤与后端 LLM 上下文过滤职责混淆，导致 UI 隐藏但模型仍污染。
- [ ] 新增依赖是否合理？未发现新增第三方依赖。
- [ ] 接口是否向后兼容？不完全。session messages 中混入内部指令后，历史会话恢复和 LLM 调用都不兼容。

### 遗留问题
- 当前 `bank_id=2` 已无未完成题，新建完整做题链路仍需干净题库或正常 API 准备测试数据。
- 由于 session 101 已被多轮测试污染，修复后建议新建干净 session 复测，避免旧数据误判。
- 仍需在修复后复测：题显框箭头 disabled/动画、`📍跳转`、think 气泡、偏好/错题卡片确认、总结后新 session 跳转和旧 session 小红点。

### 2026-06-12 21:35 后续复测：清理测试数据后的新建流程
- 测试前检查：5003 未占用，随后从 `Project/web/server.py` 启动服务；监听 PID `28200`。
- 测试账号：2 号账号，用户已清理多数测试数据并还原题目状态。
- 操作要求：每一步点击/输入后均用 snapshot 或截图确认页面状态。

| # | 操作 | 预期结果 | 实际结果 | 状态 |
|---|------|---------|---------|------|
| 1 | 首页快捷按钮“顺序做题” | 进入顺序做题并加载题目 | 成功进入 `session_id=103`，页面显示题显框、题目、输入框 | ✅ |
| 2 | 侧栏“功能 → 顺序做题” | 进入顺序做题并加载题目 | 成功进入 `session_id=104`，页面显示题显框、题目、输入框 | ✅ |
| 3 | 左上角“新建对话” | 新建做题会话并加载题目 | 成功进入 `session_id=105`，显示题目“求导数 $f'(x)$ 已知 $f(x)=x^3+2x^2-5x+3$” | ✅ |
| 4 | 在 session 105 输入 `f'(x)=3x^2+4x-5` 并发送 | 用户答案显示，教师给出辅导反馈 | 页面正常追加用户答案和教师追问；`exercise.chat` 返回 200 | ✅ |
| 5 | 点击扩展 → 生成偏好 | 成功生成偏好或 empty 结果 | 页面显示“未检测到新的偏好变化”；接口 200 | ✅ |
| 6 | 点击扩展 → 收录错题 | 成功生成错题草稿或 empty 草稿 | 页面显示“暂不收录”和“错题本草稿”，0 项时确认按钮禁用；接口 200 | ✅ |
| 7 | 点击扩展 → 总结本题 | 跳到下一题新 session，题显框显示新题，旧 session 可见状态提示 | 跳到 `session_id=106`，但题显框顶部显示整段系统 prompt；左侧会话列表未显示 #106，只显示 #105/#104/#103/#102 | ❌ |
| 8 | 在 session 106 输入 `1` 并发送 | 新题聊天可继续正常辅导 | 教师反馈能返回，但页面被题显框中的超长系统 prompt 横向撑开，右侧出现大面积黑色空白 | ❌ |
| 9 | 检查屏幕横向溢出 | 页面宽度应等于视口，无右侧黑块/横向偏移 | `body.scrollWidth=4874`，`.qn-card.qn-tap.scrollWidth=8233`，`.app` 整体 x=-529，左侧栏被挤到屏幕外 | ❌ |
| 10 | 刷新 `session_id=106` 页面 | 自动恢复到对应做题会话，且无横向溢出 | 刷新后页面回到首页，不自动进入 session；此时 `#106` 出现在会话列表，`body.scrollWidth=852` | ❌ |
| 11 | 刷新后点击左侧 `#106` | 进入会话且题显框显示题目，无右侧黑块 | 再次进入会话后题显框仍显示 system prompt，`body.scrollWidth=4874`、`.qn-card.scrollWidth=8233` | ❌ |
| 12 | 在异常布局下点击 `📍跳转` | 跳转到题目对应消息 | 点击失败，Playwright 报右侧“下一个”箭头 SVG 拦截指针事件；`.qn-jump` 与 `.qn-arrow.qn-right` 坐标重叠 | ❌ |
| 13 | 在异常布局下点击“下一个”箭头 | 切换题显框/下一条题目，布局稳定 | 题显框未切换，`body.scrollLeft` 变为 744，`.app x=-744`，页面进一步整体左移 | ❌ |
| 14 | 控制台 | 无错误 | 仍有 `Process/test/view.js` 404；本轮业务点击未出现 JS runtime error | ⚠️ |

### 2026-06-12 21:35 后续复测网络证据
| 请求 | 请求体 | 响应要点 | 结论 |
|---|---|---|---|
| #40 | `exercise.chat {session_id:105,message:"f'(x)=3x^2+4x-5"}` | 200，教师追问“能一步一步解释如何求导吗” | 新建会话聊天主流程可用 |
| #41 | `exercise.preference {session_id:105}` | 200，返回 empty/“未检测到新的偏好变化” | 偏好 empty 路径可用 |
| #42 | `exercise.collect {session_id:105}` | 200，返回 `collect_draft` 且 `items: []` | 错题 empty 草稿路径可用 |
| #43 | `exercise.summary {session_id:105}` | 200，`summary_started next_session_id:106` | 总结能创建下一题 session |
| #44 | `session.get {id:106}` | messages 第一条是完整 system prompt，第二条 user 是新题文本 | 前端恢复/展示从 system prompt 取题显框内容，导致 UI 错误 |
| #45 | `exercise.chat {session_id:106,message:"1"}` | 200，教师围绕极限题继续引导 | 新 session 后端聊天可用，但 UI 题显框仍错 |

### 本轮修正结论
- “打开服务器后不能点击顺序做题”本轮未复现：首页快捷“顺序做题”和侧栏“功能 → 顺序做题”均能进入做题。
- “新建对话之后的业务流程”多数可用：新建、答题、偏好 empty、错题 empty、总结创建下一题 session 均可走通。
- 当前最主要未通过点：总结后新 session 的题显框显示 system prompt 而不是题目，并把页面横向撑开；刷新后 `#106` 会出现在列表，但点击进入仍复现溢出。
- 屏幕右侧黑块/整体左移根因表现：题显框 `.qn-card.qn-tap` 内部渲染 system prompt，其中 `<code>`/KaTeX 长内容不换行，导致 `.qn-card.scrollWidth=8233`、`body.scrollWidth≈4874`；点击右箭头后 `body.scrollLeft=744`，`.app x=-744`。
- 异常布局还导致题显框按钮重叠：`📍跳转` 与“下一个”箭头坐标重叠，跳转按钮被右箭头 SVG 拦截，无法点击。

### 测试结论
部分通过。新建做题主流程已基本可用；主要阻断集中在总结后新 session 的 UI 恢复/题显框数据来源，以及会话列表未刷新显示新 session。

(未结案)
